spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config
      name: common, auth
      profile: default
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
server:
  port: 8100
